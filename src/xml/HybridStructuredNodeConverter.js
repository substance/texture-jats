/*
  HybridStructuredNodeConverter are used as StructuredNodes, as well as Annotations.
*/
export default
class HybridStructuredNodeConverter {

  constructor(type) {
    this.type = type
  }

  import(el, node, converter) {

  }

  export(node, el, converter) {

  }

}